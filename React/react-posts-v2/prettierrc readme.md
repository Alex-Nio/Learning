```json
{
  // Определяет ширину строки в символах.
  "printWidth": 80,

  // Указывает количество пробелов, используемых для отступов.
  "tabWidth": 2,

  // Использует табуляцию вместо пробелов для отступов.
  "useTabs": false,

  // Добавляет точку с запятой в конце каждой строки.
  "semi": true,

  // Использует одинарные кавычки вместо двойных.
  "singleQuote": true,

  // Добавляет запятую после последнего элемента в объектах, массивах и других структурах данных.
  "trailingComma": "es5",

  // Добавляет пробел между фигурными скобками и содержимым в JSX.
  "bracketSpacing": true,

  // Размещает закрывающую скобку JSX на новой строке.
  "jsxBracketSameLine": false,

  // Использует одинарные кавычки в JSX атрибутах.
  "jsxSingleQuote": false,

  // Включает или отключает автоматическое добавление скобок вокруг единственного аргумента стрелочной функции.
  "arrowParens": "always",

  // Указывает конец строки: lf для Linux/MacOS, crlf для Windows, auto для автоопределения.
  "endOfLine": "lf",

  // Включает встроенное форматирование HTML и XML.
  "htmlWhitespaceSensitivity": "css",

  // Включает поддержку вложенных свойств языка YAML.
  "proseWrap": "preserve",

  // Указывает, следовать ли глобальным настройкам редактора для отступов.
  "embeddedLanguageFormatting": "auto",

  // Перенос содержимого тега на новую строку.
  "jsxSingleLineElement": false
}
```
